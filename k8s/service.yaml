apiVersion: v1
kind: Service

metadata:
  name: microservice-service

spec:
  type: NodePort

  selector:
    app: microservice-app

  ports:
  - protocol: TCP
    port: 80
    targetPort: 3000
    nodePort: 30080